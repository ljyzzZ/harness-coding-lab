{
  "name": "harness-coding-lab",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "zod": "^4.5.2"
  },
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "check:types": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run check:types && bun test"
  }
}
